# Extra dependencies needed only to BUILD the double-click app bundles.
# Install alongside requirements-gui.txt on the machine doing the build:
#
#   pip install -r requirements-gui.txt -r requirements-build.txt
#   pyinstaller packaging/stackcopy_gui.spec
#
# PyInstaller cannot cross-compile: build the .app on macOS and the .exe on
# Windows (the GitHub Actions workflow does both automatically).
pyinstaller>=6.6
